<?php
/** op-layout-flexbox:/html.phtml
 *
 * @created     2023-05-09
 * @version     1.0
 * @package     op-layout-flexbox
 * @author      Tomoaki Nagahara <tomoaki.nagahara@gmail.com>
 * @copyright   Tomoaki Nagahara All right reserved.
 */

/** Declare strict
 *
 */
declare(strict_types=1);

/** namespace
 *
 */
namespace OP;

//  Get html config.
$html   = OP()->Config('html');

//	...
$template_dir     = RootPath('template');
$header_path      = file_exists($path = $template_dir . 'layout/header.phtml')      ? OP()->MetaPath()->Encode($path) : 'flexbox:/header.phtml';
$footer_path      = file_exists($path = $template_dir . 'layout/footer.phtml')      ? OP()->MetaPath()->Encode($path) : 'flexbox:/footer.phtml';
$menu_top_path    = file_exists($path = $template_dir . 'layout/menu/top.phtml')    ? OP()->MetaPath()->Encode($path) : 'flexbox:/menu_top.phtml';
$menu_bottom_path = file_exists($path = $template_dir . 'layout/menu/bottom.phtml') ? OP()->MetaPath()->Encode($path) : 'flexbox:/menu_bottom.phtml';
$menu_right_path  = file_exists($path = $template_dir . 'layout/menu/right.phtml')  ? OP()->MetaPath()->Encode($path) : 'flexbox:/menu_right.phtml';
$menu_left_path   = file_exists($path = $template_dir . 'layout/menu/left.phtml')   ? OP()->MetaPath()->Encode($path) : 'flexbox:/menu_left.phtml';

?>
<!doctype html>
<html lang="<?= $html['lang'] ?? 'en' ?>">
    <head>
        <?php
        Template('layout/head.phtml');
        ?>
    </head>
    <body id="body" class="body">
        <div id="header-area"><?php Template($header_path) ?></div>
        <div id="body-area">
            <div id="menu-top"><?php Template($menu_top_path) ?></div>
            <div id="menu-middle">
                <div id="menu-left" ><?php Template($menu_left_path) ?></div>
                <div id="content"   ><?php Content() ?></div>
                <div id="menu-right"><?php Template($menu_right_path) ?></div>
            </div>
            <div id="menu-bottom"><?php Template($menu_bottom_path) ?></div>
        </div>
        <div id="footer-area">
            <?php Template($footer_path) ?>
            <?php Template('flexbox:/copyright.phtml') ?>
        </div>
    </body>
</html>
